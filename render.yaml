services:
  - type: web
    name: file-transfer
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: VITE_WS_URL
        sync: false
      - key: VITE_API_URL
        sync: false
